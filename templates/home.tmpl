<!DOCTYPE html>
<html lang="{{.Lang}}">
<head>
    {{template "page-head" .}}
    {{template "nav-styles" .}}
    {{template "home-styles" .}}
</head>

<body>
{{template "nav" .}}

<canvas id="mycelium-canvas"></canvas>

<div class="page-wrapper">

{{if .User}}
    {{template "home-dashboard" .}}
   {{else}}
 {{template "home-hero" .}}
 {{template "home-flow" .}}
 {{template "home-demo" .}}
 {{template "home-steps" .}}
 {{template "home-features" .}}
 {{template "home-security" .}}
 {{template "home-cta-pricing" .}}

       {{end}}
      
      {{template "public-footer" .}}

</div>

<!-- MYCELIUM ANIMATION -->
{{template "home-scripts" .}}

</body>
</html>
