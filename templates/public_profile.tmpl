<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{.Profile.Name}} | PsiloConValley</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{.Profile.Name}} — {{.Location}}. Request a quote or pay an invoice.">
    <meta name="robots" content="index,follow">
    <link rel="canonical" href="{{.BaseURL}}/biz/{{.Slug}}">
    <meta property="og:type" content="website">
    <meta property="og:title" content="{{.Profile.Name}} | PsiloConValley">
    <meta property="og:description" content="{{.Profile.Name}} — {{.Location}}. Request a quote or pay an invoice.">
    <meta property="og:url" content="{{.BaseURL}}/biz/{{.Slug}}">
    <link rel="icon" type="image/svg+xml" href="/static/favicon.svg">
    {{template "public-profile-styles" .}}
</head>
<body>

<div class="pp-wrap">

    <!-- ── Business Card ── -->
    <div class="pp-card">
        {{if .Profile.LogoURL}}
        <img src="{{.Profile.LogoURL}}" alt="{{.Profile.Name}}" class="pp-logo">
        {{end}}
        <div class="pp-name">{{.Profile.Name}}</div>
        {{if .Location}}
        <div class="pp-location">📍 {{.Location}}</div>
        {{end}}
        <div class="pp-contact-row">
            {{if .DisplayPhone}}
            <a href="tel:{{.Profile.Phone}}" class="pp-contact-btn">
                <span class="pp-contact-icon">📞</span>
                {{.DisplayPhone}}
            </a>
            {{end}}
            {{if .Profile.Email}}
            <a href="mailto:{{.Profile.Email}}" class="pp-contact-btn">
                <span class="pp-contact-icon">📧</span>
                {{.Profile.Email}}
            </a>
            {{end}}
        </div>
    </div>

    <!-- ── Request a Quote ── -->
    <div class="pp-section">
        <div class="pp-section-title">
            <span class="pp-section-icon">💬</span>
            Request a Quote
        </div>

        {{if .QuoteSuccess}}
        <div class="pp-success">✓ {{.QuoteSuccess}}</div>
        {{end}}

        {{if .QuoteError}}
        <div class="pp-error">{{.QuoteError}}</div>
        {{end}}

        {{if not .QuoteSuccess}}
        <form method="POST" action="/biz/{{.Slug}}/quote" autocomplete="on">
            {{.csrfField}}
            <div class="pp-field">
                <label class="pp-label" for="qr-name">Your Name</label>
                <input type="text" name="client_name" id="qr-name" class="pp-input"
                       placeholder="John Smith"
                       autocomplete="name"
                       required>
            </div>
            <div class="pp-field">
                <label class="pp-label" for="qr-phone">Phone Number</label>
                <input type="tel" name="client_phone" id="qr-phone" class="pp-input"
                       placeholder="(555) 123-4567"
                       autocomplete="tel"
                       inputmode="tel">
            </div>
            <div class="pp-field">
                <label class="pp-label" for="qr-desc">What do you need?</label>
                <textarea name="description" id="qr-desc" class="pp-textarea"
                          placeholder="Describe the work you need done..."></textarea>
            </div>
            <button type="submit" class="pp-btn pp-btn-green">Send Request →</button>
        </form>
        {{end}}
    </div>

    <!-- ── Pay an Invoice ── -->
    <div class="pp-section">
        <div class="pp-section-title">
            <span class="pp-section-icon">💳</span>
            Pay an Invoice
        </div>

        {{if .InvoiceError}}
        <div class="pp-error">Invoice not found. Please check your invoice number and phone.</div>
        {{end}}

        <form method="POST" action="/biz/{{.Slug}}/invoice" autocomplete="on">
            {{.csrfField}}
            <div class="pp-field">
                <label class="pp-label" for="inv-num">Invoice Number</label>
                <input type="text" name="invoice_number" id="inv-num" class="pp-input"
                       placeholder="INV-0042"
                       required>
            </div>
            <div class="pp-field">
                <label class="pp-label" for="inv-phone">Your Phone Number</label>
                <input type="tel" name="client_phone" id="inv-phone" class="pp-input"
                       placeholder="(555) 123-4567"
                       autocomplete="tel"
                       inputmode="tel">
            </div>
            <button type="submit" class="pp-btn pp-btn-green">Find Invoice →</button>
        </form>
    </div>

    <!-- ── Branding Footer ── -->
    <div class="pp-branding">
        <div class="pp-branding-text">Professional invoicing by PsiloConValley</div>
        <a href="/register" class="pp-branding-link">Create Your Business Page →</a>
    </div>

</div>

</body>
</html>
