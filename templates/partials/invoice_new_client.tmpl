{{define "invoice-new-client"}}
                <section class="section">
                    <div class="step-badge">
                        <span class="step-dot active"></span>
                        <span class="step-dot active"></span>
                        <span class="step-dot"></span>
                        {{.T.InvoiceNewStep2}}
                    </div>
                    <h2>{{.T.InvoiceNewCustomerDetails}}</h2>

                    {{if .Clients}}
                    <div style="margin-bottom:20px;">
                        <label>{{.T.InvoiceNewAutoFill}}</label>
                        <select id="client_selector" onchange="fillClient(this)">
                            <option value="">{{.T.InvoiceNewAutoFillHint}}</option>
                            {{range .Clients}}
                            <option value="{{.ID}}"
                                data-name="{{.Name}}"
                                data-email="{{.Email}}"
                                data-address="{{.Address}}"
                                data-city="{{.City}}"
                                data-state="{{.State}}"
                                data-zip="{{.Zip}}"
                                data-country="{{.Country}}"
                                {{if $.Invoice.ClientID}}{{if eq $.Invoice.ClientID .ID}}selected{{end}}{{end}}>
                                {{.Name}}{{if .Email}} ({{.Email}}){{end}}
                            </option>
                            {{end}}
                        </select>
                    </div>
                    {{end}}

                    <input type="hidden" name="client_id" id="client_id"
                           value="{{if .Invoice.ClientID}}{{.Invoice.ClientID}}{{end}}">

                    <div class="grid-3">
                        <div>
                            <label>{{.T.InvoiceNewCustomerName}} <span style="color:var(--danger)">*</span></label>
                            <input type="text" name="client_name" id="client_name" required
                                   value="{{field .Invoice.ClientName}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderClient}}"
                                   autocomplete="off">
                        </div>
                        <div>
                            <label>{{.T.InvoiceNewEmail}}</label>
                            <input type="email" name="client_email" id="client_email"
                                   value="{{field .Invoice.ClientEmail}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderClEmail}}"
                                   inputmode="email"
                                   autocomplete="off">
                        </div>
                        <div>
                            <label>{{if eq .DocumentType "estimate"}}{{.T.InvoiceNewEstimateNumber}}{{else}}{{.T.InvoiceNewInvoiceNumber}}{{end}}</label>
                            {{if eq .Mode "edit"}}
                            <input type="text" name="invoice_number" readonly
                                   value="{{.Invoice.InvoiceNumber}}">
                            {{else}}
                            <input type="text" name="invoice_number"
                                   value="{{field .Invoice.InvoiceNumber}}"
			    placeholder="{{if eq .DocumentType "estimate"}}{{.T.InvoiceNewPlaceholderEstNum}}{{else}}{{.T.InvoiceNewPlaceholderDocNum}}{{end}}">
                            {{end}}
                            {{if .Errors}}
                            {{range .Errors}}
                            {{if eq .Field "invoice_number"}}
                            <div class="field-error">{{.Message}}</div>
                            {{end}}
                            {{end}}
                            {{end}}
                        </div>
                    </div>

                    <div class="grid-2" style="margin-top:16px;">
                        <div>
                            <label>{{.T.InvoiceNewIssueDate}}</label>
                            <input type="date" name="issue_date"
                                   value="{{field .Invoice.IssueDate}}"
                                   style="font-size:16px;">
                        </div>
                        <div>
                            <label>{{.T.InvoiceNewDueDate}}</label>
                            <input type="date" name="due_date"
                                   value="{{field .Invoice.DueDate}}"
                                   style="font-size:16px;">
                        </div>
                    </div>

                    <div class="grid-3" style="margin-top:16px;">
                        <div>
                            <label>{{.T.InvoiceNewAddress}}</label>
                            <input type="text" name="client_address" id="client_address"
                                   value="{{field .Invoice.ClientAddress}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderClAddr}}">
                        </div>
                        <div>
                            <label>{{.T.InvoiceNewCity}}</label>
                            <input type="text" name="client_city" id="client_city"
                                   value="{{field .Invoice.ClientCity}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderClCity}}">
                        </div>
                        <div>
                            <label>{{.T.InvoiceNewZip}}</label>
                            <input type="text" name="client_zip" id="client_zip"
                                   value="{{field .Invoice.ClientZip}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderClZip}}"
                                   inputmode="numeric">
                        </div>
                    </div>

                    <div class="grid-2" style="margin-top:16px;">
                        <div>
                            <label>{{.T.InvoiceNewCountry}}</label>
                            <input type="text" name="client_country" id="client_country"
                                   value="{{field .Invoice.ClientCountry}}"
                                   placeholder="{{.T.InvoiceNewPlaceholderCountry}}">
                        </div>
                        <div>
                            <label>{{.T.InvoiceNewStateRegion}}</label>
                            <select name="client_state" id="client_state">
                                <option value="">{{.T.InvoiceNewSelectState}}</option>
                                {{range .USStates}}
                                <option value="{{.Name}}" {{if eq $.Invoice.ClientState .Name}}selected{{end}}>{{.Name}}</option>
                                {{end}}
                            </select>
                        </div>
                    </div>
                </section>
{{end}}
