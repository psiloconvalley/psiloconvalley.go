<!DOCTYPE html>
<html lang="en">
<head>
    <title>Audit Log | PSILOCONVALLEY Admin</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{template "nav-styles" .}}
    {{template "base-styles" .}}
    {{template "admin-audit-styles-layout" .}}
    {{template "admin-audit-styles-table" .}}
</head>
<body>

{{template "nav" .}}

<div class="audit-wrap">

    <a href="/admin/analytics" class="back-link">← Admin Analytics</a>

    <div class="audit-header">
        <div class="audit-eyebrow">Admin</div>
        <h1>Audit Log</h1>
        <p>Every financial and authentication event — append-only, tamper-evident.</p>
    </div>

    {{template "admin-audit-filters" .}}
    {{template "admin-audit-table" .}}

</div>

{{template "public-footer" .}}

</body>
</html>
