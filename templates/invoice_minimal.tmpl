<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{.Invoice.InvoiceNumber}} — {{.Invoice.ClientName}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{template "nav-styles" .}}
    {{template "invoice-theme-minimal" .}}
</head>
<body>
{{template "invoice-doc-body" .}}
</body>
</html>
